/* tslint:disable */
/* eslint-disable */
/**
 * Weft API
 * The Weft API is the buyer-runtime surface that powers the `weft` CLI, the hosted MCP server (`weft.network/mcp`), and any third-party agent that wants to discover and pay for paid resources on Weft. v1 covers five buyer concerns:    1. Account onboarding (`/api/v1/auth/_*`, `/api/v1/me`)   2. CLI authentication (`/api/v1/api_keys`)   3. Wallet visibility (`/api/v1/balance`)   4. Discovery (`/api/v1/search`)   5. Paid execution (`/api/v1/fetch`)   6. Purchase history (`/api/v1/purchases`)  Seller-side concerns (agent management, payout analytics, webhook delivery, the public storefront for `data_api` resources) live in the dashboard and are intentionally not documented here. They will be split out into a separate, dashboard-scoped spec when they need to be SDK-consumable.  All errors share the envelope defined by `ErrorResponse`, except the buyer-runtime endpoints (`/search`, `/fetch`) which use bespoke envelopes carrying additional context — see `SearchErrorResponse` and `FetchErrorResponse`.
 *
 * The version of the OpenAPI document: 0.9.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * Price + provenance + freshness for this (endpoint × capability) pairing.
 * Always present; an unpriced endpoint carries null amounts rather than an
 * absent block, so a caller never has to distinguish "free" from "we do not
 * know".
 *
 * @export
 * @interface SearchEndpointPrice
 */
export interface SearchEndpointPrice {
    /**
     * The indexed price in USD as a decimal string (e.g. "0.008") — the
     * dollar value people and agents reason in. A decimal string, never a
     * float; trailing zeros trimmed. Null when unpriced.
     *
     * @type {string}
     * @memberof SearchEndpointPrice
     */
    indexedUsd?: string;
    /**
     * The same price in integer micro-USD — the settlement grain, exact.
     * Null exactly when `indexed_usd` is. Use this for settlement.
     *
     * @type {number}
     * @memberof SearchEndpointPrice
     */
    atomic?: number;
    /**
     * Where the amount came from. `probe` = observed live in the endpoint's
     * own 402 payment-required challenge — an observation, the strongest
     * evidence we hold. `inferred` = derived from a spec (OpenAPI) or a
     * resale edge by the extraction pipeline, so it can lag a provider's
     * re-pricing. Null when the endpoint carries no price.
     *
     * @type {string}
     * @memberof SearchEndpointPrice
     */
    source?: SearchEndpointPriceSourceEnum;
    /**
     * When the amount was observed. Null when unrecorded.
     * @type {Date}
     * @memberof SearchEndpointPrice
     */
    lastObservedAt?: Date;
    /**
     * Whether this amount was confirmed against a live 402 challenge for
     * THIS response. Search does not issue a payment-required probe at
     * query time, so this is `false` today — treat an unflagged price as
     * indexed, not verified.
     *
     * @type {boolean}
     * @memberof SearchEndpointPrice
     */
    liveVerified?: boolean;
}


/**
 * @export
 */
export const SearchEndpointPriceSourceEnum = {
    Probe: 'probe',
    Inferred: 'inferred'
} as const;
export type SearchEndpointPriceSourceEnum = typeof SearchEndpointPriceSourceEnum[keyof typeof SearchEndpointPriceSourceEnum];


/**
 * Check if a given object implements the SearchEndpointPrice interface.
 */
export function instanceOfSearchEndpointPrice(value: object): value is SearchEndpointPrice {
    return true;
}

export function SearchEndpointPriceFromJSON(json: any): SearchEndpointPrice {
    return SearchEndpointPriceFromJSONTyped(json, false);
}

export function SearchEndpointPriceFromJSONTyped(json: any, ignoreDiscriminator: boolean): SearchEndpointPrice {
    if (json == null) {
        return json;
    }
    return {

        'indexedUsd': json['indexed_usd'] == null ? undefined : json['indexed_usd'],
        'atomic': json['atomic'] == null ? undefined : json['atomic'],
        'source': json['source'] == null ? undefined : json['source'],
        'lastObservedAt': json['last_observed_at'] == null ? undefined : (new Date(json['last_observed_at'])),
        'liveVerified': json['live_verified'] == null ? undefined : json['live_verified'],
    };
}

export function SearchEndpointPriceToJSON(json: any): SearchEndpointPrice {
    return SearchEndpointPriceToJSONTyped(json, false);
}

export function SearchEndpointPriceToJSONTyped(value?: SearchEndpointPrice | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {

        'indexed_usd': value['indexedUsd'],
        'atomic': value['atomic'],
        'source': value['source'],
        'last_observed_at': value['lastObservedAt'] == null ? value['lastObservedAt'] : value['lastObservedAt'].toISOString(),
        'live_verified': value['liveVerified'],
    };
}
