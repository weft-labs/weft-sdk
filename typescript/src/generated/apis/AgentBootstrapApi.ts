/* tslint:disable */
/* eslint-disable */
/**
 * Weft API
 * The Weft API powers the `weft` CLI, the hosted MCP server (`weft.network/mcp`), and third-party applications that discover and pay for resources on Weft. The buyer runtime covers six concerns:    1. Account creation and recovery (`/api/v1/auth/_*`)   2. Credential identity (`/api/v1/me`)   3. Wallet visibility (`/api/v1/balance`)   4. Discovery (`/api/v1/search`)   5. Paid execution (`/api/v1/fetch`)   6. Purchase history (`/api/v1/purchases`)  Buyer runtime calls require a dashboard-created `wk_*` account key or an OAuth access token with the relevant scope. The organization-scoped API key and payment operations in this document are seller administration surfaces and require an `ax_live_*` resource key. The two key types are not interchangeable.  Agent bootstrap is a separate, temporary path. A `wbt_*` bootstrap bearer can call search plus its own status/cancel operations for 30 minutes. It never resolves to a User and cannot call wallet, balance, fetch, seller, organization, API-key, or MCP surfaces.  Bootstrap lifecycle successes follow the API-standard `{ \"data\": ... }` envelope. All errors share the envelope defined by `ErrorResponse`, except the buyer-runtime endpoints (`/search`, `/fetch`) which use bespoke envelopes carrying additional context — see `SearchErrorResponse` and `FetchErrorResponse`.
 *
 * The version of the OpenAPI document: 0.11.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AccountBootstrapCreatedResponse,
  AccountBootstrapRequest,
  AccountBootstrapStatusResponse,
  ErrorResponse,
  RateLimitResponse,
} from '../models/index';
import {
    AccountBootstrapCreatedResponseFromJSON,
    AccountBootstrapCreatedResponseToJSON,
    AccountBootstrapRequestFromJSON,
    AccountBootstrapRequestToJSON,
    AccountBootstrapStatusResponseFromJSON,
    AccountBootstrapStatusResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    RateLimitResponseFromJSON,
    RateLimitResponseToJSON,
} from '../models/index';

export interface CancelAccountBootstrapRequest {
    id: string;
}

export interface CreateAccountBootstrapRequest {
    accountBootstrapRequest: AccountBootstrapRequest;
}

export interface GetAccountBootstrapRequest {
    id: string;
}

/**
 *
 */
export class AgentBootstrapApi extends runtime.BaseAPI {

    /**
     * Pending and claimed bootstraps become rejected. A consumed bootstrap also revokes the delivered OAuth grant and all refresh descendants. Repeated cancellation is idempotent.
     * Cancel a bootstrap and revoke delivered OAuth access
     */
    async cancelAccountBootstrapRaw(requestParameters: CancelAccountBootstrapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountBootstrapStatusResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling cancelAccountBootstrap().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bootstrapAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/account_bootstraps/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountBootstrapStatusResponseFromJSON(jsonValue));
    }

    /**
     * Pending and claimed bootstraps become rejected. A consumed bootstrap also revokes the delivered OAuth grant and all refresh descendants. Repeated cancellation is idempotent.
     * Cancel a bootstrap and revoke delivered OAuth access
     */
    async cancelAccountBootstrap(requestParameters: CancelAccountBootstrapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountBootstrapStatusResponse> {
        const response = await this.cancelAccountBootstrapRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * When WEFT_ACCOUNT_BOOTSTRAP_ENABLED=true, creates a 30-minute, search-only bootstrap and emails a separate claim link to the supplied address. The response includes one-time device credentials and is deliberately identical for new and existing account emails and never contains the claim token. Rate limits apply per IP, normalized email, and OAuth client; request bodies over 4 KiB are rejected before parsing. Creation is disabled by default and answers 404 unless the deployment enables the preview. The bootstrap row and one-time credentials are committed only after the mail relay accepts the claim email. A retryable 503 leaves no bootstrap row behind.
     * Create a temporary agent bootstrap
     */
    async createAccountBootstrapRaw(requestParameters: CreateAccountBootstrapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountBootstrapCreatedResponse>> {
        if (requestParameters['accountBootstrapRequest'] == null) {
            throw new runtime.RequiredError(
                'accountBootstrapRequest',
                'Required parameter "accountBootstrapRequest" was null or undefined when calling createAccountBootstrap().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/account_bootstraps`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AccountBootstrapRequestToJSON(requestParameters['accountBootstrapRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountBootstrapCreatedResponseFromJSON(jsonValue));
    }

    /**
     * When WEFT_ACCOUNT_BOOTSTRAP_ENABLED=true, creates a 30-minute, search-only bootstrap and emails a separate claim link to the supplied address. The response includes one-time device credentials and is deliberately identical for new and existing account emails and never contains the claim token. Rate limits apply per IP, normalized email, and OAuth client; request bodies over 4 KiB are rejected before parsing. Creation is disabled by default and answers 404 unless the deployment enables the preview. The bootstrap row and one-time credentials are committed only after the mail relay accepts the claim email. A retryable 503 leaves no bootstrap row behind.
     * Create a temporary agent bootstrap
     */
    async createAccountBootstrap(requestParameters: CreateAccountBootstrapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountBootstrapCreatedResponse> {
        const response = await this.createAccountBootstrapRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Read bootstrap lifecycle status
     */
    async getAccountBootstrapRaw(requestParameters: GetAccountBootstrapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountBootstrapStatusResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAccountBootstrap().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bootstrapAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/account_bootstraps/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountBootstrapStatusResponseFromJSON(jsonValue));
    }

    /**
     * Read bootstrap lifecycle status
     */
    async getAccountBootstrap(requestParameters: GetAccountBootstrapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountBootstrapStatusResponse> {
        const response = await this.getAccountBootstrapRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
