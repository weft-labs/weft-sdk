/* tslint:disable */
/* eslint-disable */
/**
 * Weft API
 * The Weft API powers the `weft` CLI, the hosted MCP server (`weft.network/mcp`), and third-party applications that discover and pay for resources on Weft. The buyer runtime covers six concerns:    1. Account creation and recovery (`/api/v1/auth/_*`)   2. Credential identity (`/api/v1/me`)   3. Wallet visibility (`/api/v1/balance`)   4. Discovery (`/api/v1/search`)   5. Paid execution (`/api/v1/fetch`)   6. Purchase history (`/api/v1/purchases`)  Buyer runtime calls require a dashboard-created `wk_*` account key or an OAuth access token with the relevant scope. The organization-scoped API key and payment operations in this document are seller administration surfaces and require an `ax_live_*` resource key. The two key types are not interchangeable.  All errors share the envelope defined by `ErrorResponse`, except the buyer-runtime endpoints (`/search`, `/fetch`) which use bespoke envelopes carrying additional context — see `SearchErrorResponse` and `FetchErrorResponse`.
 *
 * The version of the OpenAPI document: 0.11.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  ResourceEnrollmentRequest,
  ResourceEnrollmentResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ResourceEnrollmentRequestFromJSON,
    ResourceEnrollmentRequestToJSON,
    ResourceEnrollmentResponseFromJSON,
    ResourceEnrollmentResponseToJSON,
} from '../models/index';

export interface EnrollResourceRequest {
    resourceEnrollmentRequest: ResourceEnrollmentRequest;
}

/**
 *
 */
export class ResourcesApi extends runtime.BaseAPI {

    /**
     * Lets an unauthenticated agent (or its operator) self-enroll a Resource as a \"ghost\" (no owning provider). The server generates the `slug` and a single-use `claim_token`; the response also carries a `claim_url` the agent hands to its human controller to claim the resource from the dashboard.  Client-supplied `slug` and `category` are silently ignored: slugs are server-generated from `name` (so callers cannot squat on reserved names), and `category` is a post-claim concern set by a real human.  Rate-limited to 10 enrollments/hour/IP.
     * Self-enroll a ghost resource (public, no auth)
     */
    async enrollResourceRaw(requestParameters: EnrollResourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResourceEnrollmentResponse>> {
        if (requestParameters['resourceEnrollmentRequest'] == null) {
            throw new runtime.RequiredError(
                'resourceEnrollmentRequest',
                'Required parameter "resourceEnrollmentRequest" was null or undefined when calling enrollResource().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/resources/enroll`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResourceEnrollmentRequestToJSON(requestParameters['resourceEnrollmentRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ResourceEnrollmentResponseFromJSON(jsonValue));
    }

    /**
     * Lets an unauthenticated agent (or its operator) self-enroll a Resource as a \"ghost\" (no owning provider). The server generates the `slug` and a single-use `claim_token`; the response also carries a `claim_url` the agent hands to its human controller to claim the resource from the dashboard.  Client-supplied `slug` and `category` are silently ignored: slugs are server-generated from `name` (so callers cannot squat on reserved names), and `category` is a post-claim concern set by a real human.  Rate-limited to 10 enrollments/hour/IP.
     * Self-enroll a ghost resource (public, no auth)
     */
    async enrollResource(requestParameters: EnrollResourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResourceEnrollmentResponse> {
        const response = await this.enrollResourceRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
