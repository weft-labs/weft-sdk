{
  "name": "@weft-labs/sdk",
  "version": "0.2.0",
  "private": true,
  "description": "Unified Weft SDK monorepo (git install helper)",
  "scripts": {
    "postinstall": "node scripts/prepare-docs.mjs"
  }
}
