# SDK Pipeline Specs

> **Status note (2026-04-29):** Originally written for [`plans/done/2026-04-sdk-pipeline-complete.md`](../../../../cto-os/plans/done/2026-04-sdk-pipeline-complete.md). The 5-spec scope was partially superseded by the candidate-led release architecture in [`plans/done/2026-04-sdk-pipeline-candidate-release.md`](../../../../cto-os/plans/done/2026-04-sdk-pipeline-candidate-release.md) (merged 2026-04-28 via weft-app PR #275 + weft-sdk PR #2). The architecture diagram now lives at `docs/architecture/sdk-pipeline.md`. Originally tracked in [`cto-os/plans/done/2026-04-sdk-pipeline-production-readiness.md`](../../../../cto-os/plans/done/2026-04-sdk-pipeline-production-readiness.md) (moved to `done/` 2026-05-25).

| # | Spec | Gap it closes | Status |
|---|------|---------------|--------|
| 01 | [Canonical spec alignment](01-canonical-spec-alignment.md) | `weft-app/docs/openapi.yaml` is stale; missing `/api/v1/resources/enroll`, `/apis/{slug}`; `Agent.category` contract mismatch | **Open** — candidate-led pipeline made `docs/openapi.yaml` canonical, but the missing pivot endpoints were never added. Decision pending in production-readiness plan. |
| 02 | [Spec-sync auth](02-spec-sync-auth.md) | `weft-sdk/sync-spec.yml` can't read private `weft-app` — 404s on every run | ✅ **Closed** in `weft-sdk/sync-spec.yml` via `actions/create-github-app-token@v1` + Contents API. Live secret config is the only remaining step (HUMAN-TASKS.md). |
| 03 | [Consumer loop-back](03-consumer-loopback.md) | No automation bumps `@weft-labs/sdk` in `weft-app` or regenerates docs | ✅ **Deferred** 2026-05-03 per [`meetings/2026-05-03-sdk-pipeline-spec-03-04-conclusion.md`](../../../../cto-os/meetings/2026-05-03-sdk-pipeline-spec-03-04-conclusion.md). SDK release cadence (~2/quarter) doesn't justify the workflow today. Dependabot for `@weft-labs/sdk` bumped to weekly so the bump PR is faster than the manual regen step. Re-trigger: next missed `docs:generate` after a release. Tracked in [`BACKLOG.md → SDK Pipeline`](../../../../cto-os/BACKLOG.md). |
| 04 | [Contract drift test](04-contract-drift-test.md) | Runtime, spec, and generated clients can diverge silently | ⛔ **Superseded** 2026-05-03. Check 1 → spec 06. Check 2 dropped (obsolete; spec is regenerated on every push). Check 3 → 1-line assert in `weft-sdk/scripts/generate-all.sh`. |
| 05 | [Release race fix](05-release-race-fix.md) | `CD: Release` beats `CD: Staging Deploy` on tag push; manual re-run required | ✅ **Closed** in `weft-app/cd_release.yml` via Docker image wait loop (60×10s for `ghcr.io/weft-labs/weft-app:$SHA`). |
| 06 | [Committee ResponseValidation](06-committee-response-validation.md) | Runtime serializer drift from `docs/openapi.yaml` invisible to CI (post-pivot incident root cause) | **Open** — supersedes spec 04 Check 1. Audit `/api/v1/*` request-spec coverage, wire `Committee::Middleware::ResponseValidation` in test env, fix surfaced drift. |

Original ordering (now historical): 01 prerequisite → 02 unblocks sync → 03 closes loop → 04 prevents regression → 05 QoL.
