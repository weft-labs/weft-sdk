# coding: utf-8

"""
    Weft API

    The Weft API is the buyer-runtime surface that powers the `weft` CLI, the hosted MCP server (`weft.network/mcp`), and any third-party agent that wants to discover and pay for paid resources on Weft. v1 covers five buyer concerns:    1. Account onboarding (`/api/v1/auth/*`, `/api/v1/me`)   2. CLI authentication (`/api/v1/api_keys`)   3. Wallet visibility (`/api/v1/balance`)   4. Discovery (`/api/v1/search`)   5. Paid execution (`/api/v1/fetch`)   6. Purchase history (`/api/v1/purchases`)  Seller-side concerns (agent management, payout analytics, webhook delivery, the public storefront for `data_api` resources) live in the dashboard and are intentionally not documented here. They will be split out into a separate, dashboard-scoped spec when they need to be SDK-consumable.  All errors share the envelope defined by `ErrorResponse`, except the buyer-runtime endpoints (`/search`, `/fetch`) which use bespoke envelopes carrying additional context — see `SearchErrorResponse` and `FetchErrorResponse`.

    The version of the OpenAPI document: 0.9.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from weft_sdk.generated.models.principal_wallet import PrincipalWallet
from typing import Optional, Set
from typing_extensions import Self

class UserPrincipal(BaseModel):
    """
    The buyer User represented by an account API key or OAuth token.
    """ # noqa: E501
    principal_type: StrictStr
    id: StrictInt
    email: StrictStr
    display_name: Optional[StrictStr] = None
    status: StrictStr
    buyer_enabled: StrictBool
    seller_enabled: StrictBool
    provisioning_status: StrictStr
    wallet: Optional[PrincipalWallet]
    __properties: ClassVar[List[str]] = ["principal_type", "id", "email", "display_name", "status", "buyer_enabled", "seller_enabled", "provisioning_status", "wallet"]

    @field_validator('principal_type')
    def principal_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['user']):
            raise ValueError("must be one of enum values ('user')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'blocked']):
            raise ValueError("must be one of enum values ('active', 'blocked')")
        return value

    @field_validator('provisioning_status')
    def provisioning_status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['pending', 'ready', 'not_applicable']):
            raise ValueError("must be one of enum values ('pending', 'ready', 'not_applicable')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of UserPrincipal from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of wallet
        if self.wallet:
            _dict['wallet'] = self.wallet.to_dict()
        # set to None if wallet (nullable) is None
        # and model_fields_set contains the field
        if self.wallet is None and "wallet" in self.model_fields_set:
            _dict['wallet'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of UserPrincipal from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "principal_type": obj.get("principal_type"),
            "id": obj.get("id"),
            "email": obj.get("email"),
            "display_name": obj.get("display_name"),
            "status": obj.get("status"),
            "buyer_enabled": obj.get("buyer_enabled"),
            "seller_enabled": obj.get("seller_enabled"),
            "provisioning_status": obj.get("provisioning_status"),
            "wallet": PrincipalWallet.from_dict(obj["wallet"]) if obj.get("wallet") is not None else None
        })
        return _obj
