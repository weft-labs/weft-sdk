# coding: utf-8

"""
    Weft API

    The Weft API is the buyer-runtime surface that powers the `weft` CLI, the hosted MCP server (`weft.network/mcp`), and any third-party agent that wants to discover and pay for paid resources on Weft. v1 covers five buyer concerns:    1. Account onboarding (`/api/v1/auth/*`, `/api/v1/me`)   2. CLI authentication (`/api/v1/api_keys`)   3. Wallet visibility (`/api/v1/balance`)   4. Discovery (`/api/v1/search`)   5. Paid execution (`/api/v1/fetch`)   6. Purchase history (`/api/v1/payments`)  Seller-side concerns (agent management, payout analytics, webhook delivery, the public storefront for `data_api` resources) live in the dashboard and are intentionally not documented here. They will be split out into a separate, dashboard-scoped spec when they need to be SDK-consumable.  All errors share the envelope defined by `ErrorResponse`, except the buyer-runtime endpoints (`/search`, `/fetch`) which use bespoke envelopes carrying additional context — see `SearchErrorResponse` and `FetchErrorResponse`. 

    The version of the OpenAPI document: 0.7.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from weft_sdk.generated.models.resource_stats import ResourceStats
from typing import Optional, Set
from typing_extensions import Self

class EnrolledResource(BaseModel):
    """
    The `:created` view of a ghost resource. Adds `stats`, the single-use `claim_token`, and the `claim_url` to the base resource shape. The claim fields appear ONLY on this enrollment response — never in list or read views. 
    """ # noqa: E501
    id: StrictInt
    name: StrictStr
    slug: StrictStr
    description: Optional[StrictStr] = None
    kind: StrictStr
    visibility: StrictStr
    wallet_address: Optional[StrictStr] = Field(default=None, description="Declared wallet address, or `null` if none was provided.")
    category: StrictStr = Field(description="Legacy v3 taxonomy field. Returns a string only for `agent`-kind resources that carry a `category` in their metadata; `null` for all other kinds and for agents without one. Always `null` on enrollment (category is a post-claim concern). ")
    verified: StrictBool
    claimed: StrictBool = Field(description="True once the resource has an owning provider. Always false at enrollment.")
    created_at: datetime
    stats: ResourceStats
    claim_token: StrictStr = Field(description="Single-use token for the magic claim URL. Surfaced only on enrollment.")
    claim_url: StrictStr = Field(description="Dashboard path the agent hands to its human to claim the resource.")
    __properties: ClassVar[List[str]] = ["id", "name", "slug", "description", "kind", "visibility", "wallet_address", "category", "verified", "claimed", "created_at", "stats", "claim_token", "claim_url"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EnrolledResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of stats
        if self.stats:
            _dict['stats'] = self.stats.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EnrolledResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "name": obj.get("name"),
            "slug": obj.get("slug"),
            "description": obj.get("description"),
            "kind": obj.get("kind"),
            "visibility": obj.get("visibility"),
            "wallet_address": obj.get("wallet_address"),
            "category": obj.get("category"),
            "verified": obj.get("verified"),
            "claimed": obj.get("claimed"),
            "created_at": obj.get("created_at"),
            "stats": ResourceStats.from_dict(obj["stats"]) if obj.get("stats") is not None else None,
            "claim_token": obj.get("claim_token"),
            "claim_url": obj.get("claim_url")
        })
        return _obj


