# coding: utf-8

"""
    Weft API

    Weft is an agent-first payments and discovery platform built on open protocols (x402 for stablecoin payments, EIP-3009 for transfer-with-authorization, ERC-8004 for agent identity). This API powers agent self-enrollment, payment flows, and the dashboard control plane.  All endpoints under `/api/v1` are organization-scoped and require a bearer API key. Errors share the envelope defined by `ErrorResponse`. 

    The version of the OpenAPI document: 0.3.2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class EnrolledResource(BaseModel):
    """
    A Resource as returned by `POST /api/v1/resources/enroll`. Extends the public Resource shape with the single-use `claim_token` and `claim_url`. These two fields appear ONLY on the enrollment response — never on list/show endpoints — and let the enrolling client hand the magic URL to its human controller. 
    """ # noqa: E501
    id: StrictInt
    kind: StrictStr
    name: StrictStr
    slug: StrictStr
    description: Optional[StrictStr] = None
    wallet_address: Optional[StrictStr] = Field(default=None, description="Provider's wallet address; null while ghost (no Provider attached).")
    category: Optional[StrictStr] = Field(default=None, description="Domain category. Only populated for `kind: agent` Resources during the v3 → provider/resource transition; null for newer kinds. ")
    visibility: StrictStr
    verified: StrictBool
    claimed: StrictBool = Field(description="True when the Resource has a Provider attached. Ghost Resources (claimed=false) are publicly discoverable but cannot be edited until a human claims them. ")
    created_at: datetime
    claim_token: StrictStr = Field(description="Single-use token. ~256 bits of entropy. Surfaced exactly once, in this response. Used to claim the Resource at `/dashboard/claims/{claim_token}`. ")
    claim_url: StrictStr = Field(description="Absolute path the agent's human controller visits to claim the Resource.")
    __properties: ClassVar[List[str]] = ["id", "kind", "name", "slug", "description", "wallet_address", "category", "visibility", "verified", "claimed", "created_at", "claim_token", "claim_url"]

    @field_validator('kind')
    def kind_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['agent', 'data_api', 'mcp_server']):
            raise ValueError("must be one of enum values ('agent', 'data_api', 'mcp_server')")
        return value

    @field_validator('visibility')
    def visibility_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['public_profile', 'private_profile', 'unlisted']):
            raise ValueError("must be one of enum values ('public_profile', 'private_profile', 'unlisted')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EnrolledResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EnrolledResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "kind": obj.get("kind"),
            "name": obj.get("name"),
            "slug": obj.get("slug"),
            "description": obj.get("description"),
            "wallet_address": obj.get("wallet_address"),
            "category": obj.get("category"),
            "visibility": obj.get("visibility"),
            "verified": obj.get("verified"),
            "claimed": obj.get("claimed"),
            "created_at": obj.get("created_at"),
            "claim_token": obj.get("claim_token"),
            "claim_url": obj.get("claim_url")
        })
        return _obj


